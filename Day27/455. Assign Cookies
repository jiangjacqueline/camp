455. Assign Cookies
Link: https://leetcode.com/problems/assign-cookies/description/

class Solution {
    public int findContentChildren(int[] g, int[] s) {
        Arrays.sort(g);
        Arrays.sort(s);
        int i, j;
        // For a cookie j, give it to the child with the largest cookie size, satisfying gi ≤ sj and gi is the oldest child.
        for(i = j = 0; i < g.length && j < s.length; j++){
            if(g[i] <= s[j]){
                i++;
            }
        }
        return i;
    }
}
