349. Intersection of Two Arrays
Link: https://leetcode.com/problems/intersection-of-two-arrays/description/

class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        if(nums1 == null || nums2 == null){
            return null;
        }

        // Create a HashSet called hash to store all unique elements from nums1.
        // This helps in O(1) average-time lookups later.
        HashSet<Integer> hash = new HashSet<>();
        for(int i = 0; i < nums1.length; i++){
            hash.add(nums1[i]);
        }

        HashSet<Integer> resultHash = new HashSet<>();
        for(int i = 0; i < nums2.length; i++){
            if(hash.contains(nums2[i]) && !resultHash.contains(nums2[i])){
                resultHash.add(nums2[i]);
            }
        }

        int size = resultHash.size();
        // Convert the HashSet<Integer> resultHash into a primitive int[] array.
        int[] result = new int[size];
        int index = 0;
        for(int num : resultHash){
            // Keep track of the index while inserting
            result[index++] = num;
        }

        return result;
    }
}
