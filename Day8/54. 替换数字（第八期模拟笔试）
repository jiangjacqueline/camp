54. 替换数字（第八期模拟笔试）
Link: https://kamacoder.com/problempage.php?pid=1064

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String s = sc.nextLine(); // Read the input string
        System.out.println(replaceDigits(s));
    }

    public static String replaceDigits(String s) {
        StringBuilder sb = new StringBuilder();

        // Iterate through each character
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                sb.append("number"); // Replace digit with "number"
            } else {
                sb.append(c); // Keep letters unchanged
            }
        }

        return sb.toString();
    }
}
